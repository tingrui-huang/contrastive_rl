# Current F4 action sensitivity: bounded numerical protocol

Sealed before new simulator/model evaluation. HEAD/local and remote: 3d997ef6dbf132b6a857108e7e08d2d6ddd514df. Since f85a5f4 only the death-construction report was added; production dynamics and models did not change. This is a read-only audit, no training or production edits, no commit/push. The attached request ends mid-sentence after “empirical quant”; no additional requirement is inferred.

## Objects

Native TwoRouteSwampWindyF4Env: p=.30, action_noise=.01, H=50, fixed goal (8.5,3.5), gamma=.95, original reward/done. Full float64 physical XY, four stored frames, current bits, dead flag and RNG are restored. One-step comparisons share explicit normal[2]/uniform[3] slots; next masks are drawn after movement/death check. Actual effective action is clip(requested_action + .01*normal,-1,1). Primary native denominator is the difference of these effective actions, including for dead contexts as a hypothetical actuator input; execution ignores it when dead. Also save legal requested-action denominators to distinguish the model's input metric. Native float64 physical differences are the primary reliability check, float32 visible F4 is reported too.

At fixed pre-state F4 the six historical successor entries must match exactly, so next-F4 and next-XY Euclidean differences must agree. Derive free-flight J=10*.1*I=I only for alive contexts, unsaturated actuator, and all substeps accepted. Instrument collision accept/reject signatures without changing dynamics. Death switching is a separate hidden endpoint; continuation separation is not a one-step constant.

## Fixed contexts, actions and coupling

Natural bank: 112 episodes of 50 native steps (5,600 calls): 80 original noisy teacher episodes (force-safe .05, nonzero-action noise .15), 32 uniform-action episodes. Environment seed 26091301, behavior seed 26091302. Eligible contexts t<=45. Select at most 24 per stratum by fixed RNG 26091303 before paired outcomes: free, wall-near, pre-hazard, hazard-interior, hazard-boundary, dead, alive-waiting. Strata may overlap and are not pooled into a population estimate. Preserve source episode/behavior identifiers; label shortages. Collection is not actor evaluation.

Targeted bank: nine named settings, each with eight independent noise tapes: free; upper wall; corner at (1.95,2.95) where axiswise collision rejection can change; pre-hazard at (2.95,3.5); hazard interior; entry boundary at (2.5,3.5); confirmed dead; alive waiting; actuator clipping. Reach positions through actual native reset/prefix steps (zero setup noise and explicitly controlled masks), never teleport or fabricate F4. Setup intervention is clearly labeled targeted and excluded from natural population statements. Alive hazard-interior can be reached under clear entry bits, with an active mask drawn at the end of that step. Dead contexts must die through a native step. Target setup cap 1,024 calls.

Five epsilons {.1,.03,.01,.003,.001}; directions e1, e2 and two reproducible random unit vectors per context. Symmetric action pairs clip(a +/- epsilon*d/2,-1,1). Boundary target centers subtract the known shared actuator noise so the intended effective action straddles the known branch boundary, explicitly a targeted design. Natural centers are their recorded behavior action and do not adapt to paired outcomes. Seeds 26091304 (native pairs) and 26091305 (target pairs). At most 4,800 pairs / 9,600 native calls.

Validation allocation at most 512 native calls: same-snapshot/action/tape replay, original-RNG replay, changed-normal active-noise check, and instrumented/uninstrumented equality. Do not count zero denominators as ratios. Save traces for largest reliable witnesses, including actions, effective actions, physical/visible successors, pre/post masks/death, collision signature, time and source context.

## Continuation

Select up to 64 death-switching pairs from the completed fixed panel by context/epsilon/direction order (not largest divergence), at most one pair per context, requiring at least four steps remain. Continue each saved first successor for three additional native steps under identical open-loop requested actions [(1,0),(1,0),(1,0)] and independent time-indexed shared normal/uniform tapes, seed 26091306. Report F4 and XY separation at 1/2/4 total steps divided by the INITIAL effective action difference. At most 512 native calls including a repeat check. No feedback policy and no horizon extension.

## Learned models

Use the existing F4 deterministic_mse best checkpoint. Differentiate predicted raw next XY = current XY + predicted delta before the production geometry guard, with BOTH observational action slots equal to the raw action inside the function and all input/output normalizations included. This is an unconstrained deterministic predictor Jacobian, not stochastic law smoothness. Up to 256 context/action Jacobians (cap 512 including validation), with raw-coordinate finite differences on the first 64 points at h=.003 and .001 (one-sided at action bounds), at most 1,024 prediction evaluations. No synthetic weights.

ETT: four actual E26 final parameter arrays (frobenius/spectral training, seeds 0/1), each emitted under both norm implementations using the SAME underlying 48 parameters. Frozen inherited diagonal, K5 nominal and actor files are hashed; the actor is not trained or needed for paired probes. Select 64 contexts from the native bank in fixed stratum round-robin order; use their behavior action as off-diagonal center and one frozen nominal draw as x'. For each context, five epsilons and the same four directions. Primary conditions: center=x' (one action-pair center on the diagonal), and center=behavior action (off-diagonal); x' stays fixed for BOTH successors. Each corresponding pair uses exactly the same generator key. Identical-key replay and stationary atom/anchor equality verify coupling. Diagonal-centered finite differences must not be mislabeled as both points satisfying x=x'.

Also compare movement ALONG the observational diagonal, changing x and x' together, using the same key. It changes mixture probabilities/components as well as predicted displacements and is not constrained by the partial-action proof. Up to 51,200 paired ETT successor evaluations plus 1,024 validation successors. Record response block/operator/Frobenius norms, normalization activation, effective gated norms and projection/atom frequencies. Model-output cap 60,000, including deterministic predictions; Jacobian cap 512 counted separately. Count successor outputs, not API batch calls. Do not load the old 2D MLP as current F4 evidence.

## Summaries and interpretation

Per stratum/epsilon/event status: n, zero denominator count, median/p90/p95/p99/p99.9/max, fractions >1,1.05,1.25,2,5. Exceedance tolerance: ratio > threshold+.002. For native reliability, save absolute visible-vs-float64 discrepancy and a conservative float32 roundoff allowance 2e-6/denominator; classify excess over 1 as physical only if the float64 ratio also exceeds 1+.002. No tiny-perturbation ratio is proof by itself. All ratios in maze units per raw/effective action unit as labeled; no standard-deviation units.

Report natural stratum distributions separately from targeted probes and distinguish collision-signature changes and death-switching pairs. Analyze code and cross-epsilon witnesses when ratios grow. Do not infer optimal Wasserstein distances, TV smoothness, death-probability smoothness or a conditional ETT posterior from shared-noise comparisons. No global constant from a finite observed maximum. A proposed empirical L must name its sampled stratum, epsilon and coverage; do not promote it to a guarantee.

## Hard budgets and provenance

Native allocations: collection 5,600 + setup 1,024 + pairs 9,600 + validation 512 + continuation 512 = 17,248 maximum. Hard stop at that allocation (below user cap 200,000). Model successor/predictor evaluations: 60,000 maximum (below 200,000). Jacobian evaluations: 512 maximum (below 5,000). Counters charge before each operation, including validation and failed calls; save progress between phases. No budget extension, training, outcome-driven context expansion or repeated audit sweep. Record source/checkpoint hashes before execution and verify unchanged afterward. Preserve this protocol and all historical outputs.
