# Supervised reference for the original stochastic PointMaze ETT

Sealed before training/evaluation on 2026-09-14. Local and remote HEAD: 6aef461cb633cc11b84d75f790fc608c7fd85205. No newer committed supervised ETT experiment was found. All new artifacts are English. No commit/push or production-file changes are authorized by this experiment.

## Question and inputs

Can actual off-diagonal successor supervision improve the ORIGINAL random generator while maintaining diagonal fit? This is not pessimistic search and does not seek failure-only outputs. Use all existing paired_transitions.npz rows from C:/Users/trhua/Documents/Codex/2026-09-08/f/outputs/supervised_ett_native_probe_v1. Preserve episodes 0-71 for training and 72-95 for development evaluation. The latter episodes were inspected by the earlier mean-model experiment; this is not an untouched confirmatory test. Exact xb==xq in both coordinates defines diagonal, not query number. Counts from schema inspection: train diagonal/off=3860/10540; development=1269/3531. No outcome exclusion or adjacent-step split.

Read the original collector: xb is actual same-context noisy teacher advice; xq is executed on a native snapshot clone. Half the prefixes follow the teacher, half a blind controller with a shadow teacher. This intervention population differs from the historical nominal-training population. Original tuples supply xb throughout supervised fitting and one-step evaluation. Death flags/rewards enter evaluation grouping only, never loss inputs or training selection.

## Models, losses and fixed optimization

Use ett.pointmaze_region_pilot.Kernel with its original atom/K3 diagonal, geometry rectangle, eight fixed gates, and 32 Frobenius-normalized response coordinates, L=1. Both arms start at theta=zeros(48), with the SAME inherited original diagonal checkpoint used by region-pilot setup. Verified diagonal and nominal checkpoint paths/hashes are recorded in provenance. Checkpoint compatibility is established; no z-v1 substitute or new neural network. L=1 is a model-family restriction, not an exact native guarantee at collision thresholds.

A: train the original 16 diagonal-head offsets only. B: train those offsets and 32 response parameters. A's response remains exactly zero. API order is sample(theta,s,EXECUTED xq,NATURAL xb,key,K). Both losses use the unchanged emitted-XY energy_score U-statistic with independent draws within each K-sample set. L_A=mean_diagonal ES; L_B=mean_diagonal ES + mean_off ES; lambda=1. The two terms are separately averaged, in maze-distance units. No emitted log_prob, critic, reward objective, new geometry, or death head.

Training seeds 0,1, each A/B with identical initial theta and empty Adam moments; 120 updates each, no early stopping/checkpoint selection. Each update samples 128 diagonal rows and 128 off rows uniformly with replacement, using the same row/random streams across arms within seed. Only B evaluates the off term. Eight Gaussian antithetic parameter directions (16 signed candidates), head sigma=.01 and response sigma=.1. Reuse the original parameter-perturbation estimator, including categorical/atom/projection changes. Set the diagonal response-gradient component to its exact structural zero; set all A response coordinates to zero. Adam betas=.9/.999, eps=1e-8; head LR=.01, response LR=.05; update-vector caps .03 and .1 respectively. No diagonal rejection guard or weight sweep; preservation is an evaluation criterion, not an extra constraint hiding tradeoffs. The estimator targets a Gaussian-smoothed objective, not a complete pathwise gradient of the unsmoothed stochastic emitter.

Each signed loss uses K=8 draws and common keys across +/- candidates and arms. Training cost exactly 11,796,480 successors: two seeds * 120 updates * 16 candidates * 128 rows * 8 draws * (one term for A + two terms for B). Same optimization budget per arm (updates/directions/diagonal batch); B necessarily spends extra successor calls on its additional supervised term. Report this computational difference.

## One-step evaluation and decisions

Evaluate unchanged initialization plus all four fixed final checkpoints on all 4,800 development rows with K=64 independent evaluation draws, same evaluation keys across models. These draws are independent of training. Report emitted ES, finite-MC sample-mean XY vector RMSE, mean displacement, projection/atom frequency, legal positions, F4 shift, and stationary-sample fraction. A legal distribution can have an illegal mean; check both separately without modifying outputs.

Report diagonal/off separately, each for all rows and overlapping evaluation-only strata: alive outside radius-2 goal region; ordinary moving nonfatal transitions (alive before/after, positive XY displacement); fatal onset; already dead. Also split teacher/blind prefix populations. Do not call low onset XY error death identification; the generator has no validated death label. Already-dead stationary samples are one-step diagnostics, not persistent absorption certificates.

Paired B-A intervals: 2,000 bootstrap resamples of the 24 original development episode IDs, keeping all descendant rows/draws grouped and recomputing RMSE from weighted squared error. Report absent strata/replicates explicitly. Intervals condition on each fixed training seed; two seeds do not establish general seed reliability. No significance-only success claim. A one-step success requires BOTH seeds: off ES reduction >=.02 and >=10% of A's positive off ES, upper paired 95% CI<0; B-A diagonal ES upper CI<=.02; B-initial diagonal ES upper CI<=.02; off mean-RMSE not worse; and zero invalid outputs/F4 violations. Primary target is all off rows; alive-outside results must be reported prominently, not replaced by a majority-dead aggregate. If a criterion fails, report the tradeoff and stop before rollout. No post-result settings adjustment or confirmatory data collection in this budget.

## Conditional short rollout (only if the one-step gate passes)

Before training, extract and save actual prefix action/state/advice sequences: query=0 for teacher prefixes, query=1 for blind prefixes; development episodes 72-95, start times 0/5/10/20/35/46, horizon 4. Verify each recorded y equals the following prefix state. Preserve all roots/outcomes. Five models (initial/A0/B0/A1/B1), 64 paths/root, same open-loop EXECUTED action sequence and matched model randomness. At each generated state sample x' from the frozen ORIGINAL state-goal K5 nominal. Never attach recorded native advice or death labels after branch divergence.

This deliberately declares the historical nominal population. It cannot supply the native shadow-teacher latent conditioning on generated paths, so model-versus-native curves describe different continuation laws, NOT conditional ETT rollout accuracy. Report XY distributions, movement/near-stationary fractions and gaps against saved native sequences as descriptive diagnostics only; for native-already-dead roots report continued model motion with that limitation. No death rate invented for the model. This phase adds no native collection and no actor experiment. A truly matched conditional rollout test would require a validated branch-local advice/latent construction beyond these tuples.

## Budgets, checks and provenance

Training 11,796,480 successors; initial/final one-step evaluation 1,536,000; invariant checks cap 30,000; conditional rollout at most 184,320 (5*24*6*64*4). Total successor hard cap 13,546,800. No native transitions. No actor/critic/nominal training. Charges occur before calls, including failed/validation calls; no budget extension. Component checks compare original sample routing and independent NumPy energy U-statistic, exact diagonal identity, F4 history, valid geometry and fixed-key action coupling, using trained weights (never synthetic trained-model substitutes).

Store exact source/data/checkpoint hashes, split IDs, original zero theta, sealed rollout action sequences, optimizer trajectories and final checkpoint samples. Preserve source files and historical outputs. Independent saved-array verification reconstructs scoring and comparisons without additional calls. If execution fails, retain the error/counters and resume only unfinished work; do not silently restart a budget. The earlier mean baseline is reused solely as motivation, not retrained or treated as a distribution.
